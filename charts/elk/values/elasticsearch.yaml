elasticsearch:
  replicas: 1
  createCert: false        # ← stops cert secret creation attempt
  protocol: http           # ← forces http, not https
  esConfig:
    elasticsearch.yml: |
      cluster.name: "minikube-elk"
      network.host: "0.0.0.0"
      xpack.security.enabled: false
      xpack.security.transport.ssl.enabled: false
      xpack.security.http.ssl.enabled: false
      xpack.monitoring.enabled: false
  esJavaOpts: "-Xmx2g -Xms2g"
  resources:
    requests:
      cpu: "1000m"
      memory: "3Gi"
    limits:
      cpu: "2000m"
      memory: "4Gi"
  volumeClaimTemplate:
    accessModes: ["ReadWriteOnce"]
    resources:
      requests:
        storage: "10Gi"
  antiAffinity: "soft"
  persistence:
    enabled: true