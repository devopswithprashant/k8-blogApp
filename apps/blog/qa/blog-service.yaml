apiVersion: v1
kind: Service
metadata:
  name: blog-service
  namespace: dwp-qa
  labels:
    app: blog-backend
spec:
  selector:
    app: blog-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP